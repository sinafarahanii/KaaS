apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kaas-api.fullname" . }}-test-connection"
  labels:
    {{- include "kaas-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kaas-api.fullname" . }}:{{ .Values.app.service.port }}']
  restartPolicy: Never
